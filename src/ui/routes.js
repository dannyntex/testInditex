/**
 * Mapa de rutas: path -> { component, loader }.
 * Compartido entre cliente (BrowserRouter) y servidor (StaticRouter).
 * TODO: implementar en el hito de esqueleto SSR (1b) y vistas (4-6).
 */

export const routes = [];
