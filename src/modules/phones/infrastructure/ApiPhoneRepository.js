/**
 * Adaptador (servidor): implementa `PhoneRepository` hablando con la API externa.
 * Añade `x-api-key`. Solo se usa en el servidor (BFF y loaders de SSR).
 * @implements {import('../domain/PhoneRepository').PhoneRepository}
 * TODO: implementar en el hito de infraestructura (3), tras verificar el contrato de la API.
 */

export {};
