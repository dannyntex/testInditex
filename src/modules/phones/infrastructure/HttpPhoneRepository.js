/**
 * Adaptador (cliente): implementa `PhoneRepository` hablando con nuestro BFF (`/api/phones`).
 * Sin `x-api-key`. Solo se usa en el cliente (hooks).
 * @implements {import('../domain/PhoneRepository').PhoneRepository}
 * TODO: implementar en el hito de infraestructura (3).
 */

export {};
