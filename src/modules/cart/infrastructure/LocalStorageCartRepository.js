/**
 * Adaptador por defecto: implementa `CartRepository` usando `localStorage` (cliente).
 * @implements {import('../domain/CartRepository').CartRepository}
 * TODO: implementar en el hito de infraestructura (3).
 */

export {};
